Создай коммит по текущим изменениям.

1. Запусти `git status` и `git diff --staged` (если ничего не staged — `git diff`)
2. Сгенерируй сообщение на английском в стиле Conventional Commits:
   - `feat:` — новая функциональность
   - `fix:` — исправление бага
   - `refactor:` — рефакторинг без изменения поведения
   - `docs:` — документация
   - `chore:` — рутина (зависимости, конфиги, структура)
   - `ui:` — изменения интерфейса (для этого проекта)
   - `fw:` — изменения прошивки (для этого проекта)
3. Покажи итоговое сообщение и список файлов ПЕРЕД коммитом
4. После подтверждения — `git add` нужных файлов и `git commit`
5. Не пушить без явной просьбы
